name: Deploy Jekyll with GitHub Pages dependencies preinstalled

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["master"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Build job
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Identify .zip file
        id: zipfile
        run: |
          ZIP_FILE=$(ls ./Writerside/*.zip | head -n 1)  # Assuming there is one .zip file
          echo "ZIP_FILE=${ZIP_FILE}" >> $GITHUB_ENV

      - name: Remove existing directory
        run: |
          DIR_NAME=$(basename ${{ env.ZIP_FILE }} .zip)
          echo "Removing existing directory: ./Writerside/${DIR_NAME}"
          rm -rf ./Writerside/${DIR_NAME}

      - name: Unzip the new file
        run: |
          echo "Unzipping ${ZIP_FILE} into Writerside"
          unzip -o "${ZIP_FILE}" -d "./Writerside"

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Build with Jekyll
        uses: actions/jekyll-build-pages@v1
        with:
          source: ./Writerside
          destination: ./_site

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3

  # Deployment job
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
